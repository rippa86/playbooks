- name: Update the 'voltron-net'
  hosts: localhost
  tasks:
    - name: Verify the updated variable
      ansible.builtin.debug:
        var: domains
        var: optimus_au
        var: voltron_net
        var: gundam_nz
